# وثيقة متطلبات المنتج (PRD)
## تطبيق تفاعلي لتعلّم الاقتصاد الكلي المعمّق (Deep Macroeconomics Interactive Lab)

**النسخة:** 1.0
**اللغة:** العربية (RTL) بالكامل، مع الترميز الرياضي بالإنجليزية/اللاتينية كما هو معتاد أكاديميًا
**التقنية:** Python + Streamlit
**يكمّل:** تطبيق "مختبر الاقتصاد الجزئي المعمّق" السابق — نفس الفلسفة التصميمية والبنية التقنية، بمحتوى الاقتصاد الكلي المعمّق هذه المرة.

---

## 1. نظرة عامة

تطبيق ويب تفاعلي (Streamlit) يشرح مقرر "الاقتصاد الكلي المعمّق" — من مداخل الاقتصاد الكلي والحسابات الوطنية، مرورًا بالتحليلين الكلاسيكي والكينزي، فنموذج الدخل-الإنفاق (2/3/4 قطاعات)، فنموذج IS-LM، فنموذج IS-LM-BP (موندل-فليمنغ) للاقتصاد المفتوح، فالدورات الاقتصادية، وانتهاءً بنماذج النمو الاقتصادي (سولو والنمو الداخلي) — بأسلوب **نظرية + محاكي تفاعلي** لكل مفهوم، تمامًا كتطبيق الاقتصاد الجزئي.

المحتوى العلمي مبني على مطبوعات جامعية جزائرية (تحديدًا سلسلة الأستاذ عقبة عبداللاوي — جامعة الوادي — "التحليل الكينزي"، "مفاهيم أساسية في النظرية الاقتصادية الكلية"، "سلسلة ملخصات في الاقتصاد الكلي: IS-LM"، "تطبيقات التحليل الاقتصادي الكلي"، "سلسلة ملخصات وتمارين الاقتصاد الكلي المعمق: IS-LM-BP"، وبحث حول نماذج النمو الداخلي) لضمان التطابق التام مع الرموز والصيغ والترتيب المنهجي الذي يعتمده الطالب الجزائري/العربي.

## 2. المشكلة والهدف

الاقتصاد الكلي المعمّق مادة كثيفة رياضيًا (اشتقاقات جبرية طويلة لمعادلات IS وLM وBP، مضاعفات متعددة حسب كل حالة) وتتطلب فهم كيف تتزحزح المنحنيات عمليًا عند تغيّر أداة سياسة واحدة. الهدف: تحويل كل اشتقاق جبري إلى **تجربة تفاعلية** يرى فيها الطالب المنحنى يتزحزح لحظيًا مع كل سلايدر، ويقارن النتيجة العددية بالصيغة المغلقة التي درسها.

## 3. المستخدمون المستهدفون

- طلبة ماستر/ليسانس اقتصاد (مقياس اقتصاد كلي معمّق).
- الأساتذة كأداة عرض في المدرج لشرح إزاحة منحنيات IS/LM/BP.
- امتداد ثانٍ لمنظومة **EconometricsLab** إلى جانب مختبر الاقتصاد الجزئي، لتغطية المقياسين الأساسيين لطالب الاقتصاد الناطق بالعربية.

## 4. المبادئ التصميمية

نفس مبادئ تطبيق الاقتصاد الجزئي (راجع PRD_تطبيق_الاقتصاد_الجزئي_المعمق.md، القسم 4)، وتحديدًا:
1. لكل مفهوم صفحة واحدة: نظرية مختصرة ثم محاكي.
2. الرياضيات ظاهرة دائمًا مع كل سلايدر (المضاعف، معادلة IS/LM الجديدة، إلخ).
3. عربية أولاً RTL بالكامل.
4. بدون سحابة خارجية.
5. نمط موحّد لكل محاكي: أدخل المعطيات → شاهد المنحنى يتزحزح لحظيًا → اقرأ التفسير الآلي.
6. **خاص بهذا التطبيق:** كل محاكي "سياسة اقتصادية" يعرض دائمًا ثلاث حالات متجاورة (قبل الصدمة، بعد الصدمة، والفرق Δ) لأن هذا هو النمط المتكرر عبر كل فصول المرجع (IS، IS-LM، IS-LM-BP).

## 5. البنية التقنية المقترحة

يُبنى هذا التطبيق كوحدة (workspace) ثانية داخل نفس مشروع `econ_lab` المستخدم لتطبيق الاقتصاد الجزئي، أو كمشروع مستقل `macro_lab` بنفس البنية إن فُضّل الفصل:

```
macro_lab/
├── app.py
├── utils/
│   ├── rtl.py                  # نفس دالة inject_rtl() من مشروع الاقتصاد الجزئي
│   ├── plotting.py             # دوال رسم موحّدة (IS/LM/BP، AD-AS، خط 45°)
│   └── solvers.py              # حلول جبرية لأنظمة IS-LM وIS-LM-BP (sympy/numpy)
├── pages/
│   ├── 1_مدخل_وحسابات_وطنية/
│   │   ├── 1_مدخل_للاقتصاد_الكلي.py
│   │   ├── 2_حسابات_الناتج_الوطني.py
│   │   └── 3_الناتج_الحقيقي_والرقم_القياسي.py
│   ├── 2_التحليل_الكلاسيكي_والكينزي/
│   │   ├── 1_فرضيات_المدرستين_والعرض_الكلي.py
│   │   └── 2_نظرية_كمية_النقود.py
│   ├── 3_النموذج_الكينزي_البسيط/
│   │   ├── 1_دالتا_الاستهلاك_والادخار.py
│   │   ├── 2_التوازن_في_اقتصاد_قطاعين.py
│   │   ├── 3_التوازن_في_اقتصاد_ثلاث_قطاعات.py
│   │   ├── 4_التوازن_في_اقتصاد_أربع_قطاعات.py
│   │   ├── 5_المضاعفات.py
│   │   ├── 6_الفجوة_الانكماشية_والتضخمية.py
│   │   ├── 7_الميزانية_العامة_ومضاعف_الميزانية_المتوازنة.py
│   │   └── 8_الميزان_التجاري.py
│   ├── 4_نموذج_IS_LM/
│   │   ├── 1_اشتقاق_منحنى_IS.py
│   │   ├── 2_اشتقاق_منحنى_LM.py
│   │   ├── 3_التوازن_الآني_وحالات_LM.py
│   │   ├── 4_فاعلية_السياسة_المالية_والنقدية.py
│   │   └── 5_تنسيق_السياسات_الاقتصادية.py
│   ├── 5_نموذج_IS_LM_BP/
│   │   ├── 1_ميزان_المدفوعات.py
│   │   ├── 2_التوازن_الداخلي_والخارجي.py
│   │   ├── 3_السياسات_تحت_أنظمة_الصرف.py
│   │   └── 4_شرط_مارشال_ليرنر.py
│   ├── 6_الدورات_الاقتصادية/
│   │   └── 1_التغذية_العكسية_وآثار_الصفقات_التجارية.py
│   └── 7_نماذج_النمو_الاقتصادي/
│       ├── 1_نموذج_سولو.py
│       └── 2_نماذج_النمو_الداخلي.py
└── requirements.txt
```

**تقنيات الحل الرياضي:**
- `sympy` لحل الأنظمة الخطية (IS=LM، IS=LM=BP) رمزيًا وعرض خطوات الحذف بالتعويض تمامًا كما في المرجع.
- `numpy`/`scipy` لحساب القيم العددية وتحديث الرسوم البيانية لحظيًا.
- `plotly` للرسوم التفاعلية (منحنيات IS/LM/BP، خط 45°، مربعات المضاعف).

## 6. تفصيل الوحدات التعليمية والمحاكيات

### الوحدة 1 — مدخل للاقتصاد الكلي والحسابات الوطنية

**1.1 مدخل للاقتصاد الكلي**: تعريف الاقتصاد الكلي مقابل الجزئي، أهداف السياسة الاقتصادية الكلية (تحقيق التشغيل الكامل، استقرار الأسعار، النمو، توازن ميزان المدفوعات). لا يحتاج محاكيًا — بطاقات تعريفية تفاعلية فقط (اختيار هدف فيظهر شرحه).

**1.2 حسابات الناتج الوطني**: طرق قياس الناتج الثلاث (الإنتاج، الإنفاق، الدخل).
- محاكي: جدول بيانات قابل للتعديل (قيمة الإنتاج/القيمة المضافة لكل قطاع، أو بنود الإنفاق C+I+G+X-M، أو عناصر الدخل أجور+أرباح+ريع+فائدة) يحسب الناتج بالطرق الثلاث ويُظهر أنها متساوية (Y مطابقة أفقيًا).

**1.3 الناتج الحقيقي والرقم القياسي**: الفرق بين الناتج الاسمي والحقيقي، الانكماش الناتج عن التضخم.
- محاكي: سلايدر لسنة الأساس وسنة المقارنة (كميات وأسعار)، حساب الناتج الاسمي والحقيقي ومعامل الانكماش (deflator) تلقائيًا، بنفس منطق مؤشرات الأسعار في تطبيق الاقتصاد الجزئي (لاسبير/باش) إن أراد المستخدم المقارنة.

### الوحدة 2 — التحليل الكلاسيكي والكينزي

**2.1 فرضيات المدرستين ومنحنى العرض الكلي**
- نظرية: فرضيات الكلاسيك (آلية السعر الحر، المرونة الكاملة للأجور والأسعار، قانون ساي، ثبات سرعة دوران النقود)؛ فرضيات الكينزيين (جمود الأجور نحو الأسفل، الطلب الفعّال، عدم استقرار الاستثمار الخاص). منحنى العرض الكلي عمودي عند الكلاسيك (عند مستوى التشغيل الكامل) وأفقي/على شكل خط °45 عند الكينزيين قبل بلوغ التشغيل الكامل.
- محاكي: مفتاح تبديل "كلاسيكي / كينزي"؛ يرسم منحنى AS وفق الاختيار (عمودي عند Yf، أو أفقي) مع منحنى AD قابل للإزاحة بسلايدر، ويوضح أثر إزاحة AD على السعر مقابل الكمية في كل حالة (في الحالة الكلاسيكية: يتغير السعر فقط؛ في الحالة الكينزية قبل التشغيل الكامل: يتغير الناتج فقط).

**2.2 نظرية كمية النقود**
- نظرية: `M·V = P·Y`؛ عند الكلاسيك V وY ثابتان في المدى القصير فتتناسب P طرديًا مع M.
- محاكي: سلايدرات لـ M وV وY، حساب P تلقائيًا، ورسم أثر زيادة M على P فقط (تأكيدًا لحياد النقود الكلاسيكي).

### الوحدة 3 — النموذج الكينزي البسيط (اقتصاد الدخل-الإنفاق)

**3.1 دالتا الاستهلاك والادخار**
- نظرية: `C = a + bYd`, `S = -a + sYd` حيث `s=1-b`؛ `MPC=ΔC/ΔY`, `APC=C/Y=a/Y+MPC`؛ وبالمثل للادخار `MPS`, `APS`.
- محاكي: سلايدرات a، b؛ رسم دالتي الاستهلاك والادخار معًا مقابل الدخل، مع عرض MPC/APC/MPS/APS عند دخل مُختار بمؤشر تفاعلي على المحور الأفقي.

**3.2 التوازن في اقتصاد قطاعين**
- نظرية: `Y=C+I` أو `S=I`؛ `Y*=1/(1-b)[a+I0]`؛ التمثيل البياني (خط °45 مع C+I، ومخطط S,I).
- محاكي: سلايدرات a، b، I0؛ رسم مزدوج (الرسم العلوي: AD=C+I مقابل خط °45؛ الرسم السفلي: S وI) مع نقطة التوازن E ومستوى Y* محسوبًا آنيًا.

**3.3 التوازن في اقتصاد ثلاث قطاعات**
- نظرية: إضافة القطاع الحكومي (G0، وT إما مستقلة `T0` أو مرتبطة بالدخل `T0+tY`، وR0)؛ الدخل المتاح `Yd=Y-T+R`؛ `Y*=1/(1-b)[a+I0+G0-bT0+bR0]` (ضرائب مستقلة) أو `Y*=1/(1-b+bt)[...]` (ضرائب مرتبطة بالدخل).
- محاكي: مفتاح تبديل "ضرائب مستقلة / ضرائب مرتبطة بالدخل"؛ سلايدرات a، b، I0، G0، T0، R0، وt عند الاختيار الثاني؛ رسم AD وخط °45، ومخطط الموارد S+T مقابل الاستخدامات I+G+R، مع مقارنة جنبًا إلى جنب لأثر كل نمط ضريبي على حدة الميل (كما في الرسم البياني المزدوج E1/E2 بالمرجع).

**3.4 التوازن في اقتصاد أربع قطاعات (اقتصاد مفتوح)**
- نظرية: إضافة قطاع العالم الخارجي (`X=X0`، `M=M0+mY`)؛ `Y=C+I+G+X-M`؛ `Y*=1/[1-b+bt+m][a+I0+G0-bT0+bR0+X0-M0]`.
- محاكي: نفس محاكي 3.3 مع إضافة X0 وM0 وm، وعرض المضاعف الجديد `Ke=1/[1-b+bt+m]` مقارنة بمضاعف الاقتصاد المغلق، مع رسم NX=X-M على مخطط جانبي.

**3.5 المضاعفات**
- نظرية: مضاعف الاستثمار/الاستهلاك التلقائي `Ke=1/(1-b)`، مضاعف الإنفاق الحكومي، مضاعف التحويلات `KeR=b/(1-b)`، مضاعف الضرائب `KeT=-b/(1-b)` (وصيغها المعدّلة بوجود t وm)، مضاعف الصادرات/الواردات.
- محاكي: جدول تفاعلي لكل مضاعف (اختيار b، t، m من سلايدرات مشتركة) يُحدّث كل الصيغ السبع معًا؛ إدخال Δ لأي أداة (ΔG0، ΔT0، ΔR0، ΔI0، Δa، ΔX0، ΔM0) وعرض ΔY الناتج فورًا مع رسم شريطي (bar chart) يقارن أثر كل أداة على Y بنفس مقدار الصدمة، لإبراز أن مضاعف الضرائب/التحويلات أصغر من مضاعف الإنفاق المباشر.

**3.6 الفجوة الانكماشية والفجوة التضخمية**
- نظرية: `Gap = ΔY/Ke = |Yf-Y*|/Ke`؛ فجوة انكماشية إذا Y*<Yf، تضخمية إذا Y*>Yf.
- محاكي: سلايدر لـ Yf ومدخلات النموذج (من 3.3/3.4)؛ التطبيق يحسب Y* تلقائيًا، يحدد نوع الفجوة، ويرسمها بصريًا على مخطط AD/°45 (المسافة الرأسية AB أو DM كما في رسوم المرجع)، ويحسب مقدار الإنفاق المستقل الإضافي اللازم لسدّها.

**3.7 الميزانية العامة ومضاعف الميزانية المتوازنة**
- نظرية: `BS=T-G-R`؛ حالات التوازن/الفائض/العجز؛ أثر الميزانية المتوازنة المتعادلة `ΔY=ΔG0=ΔT0` عند ضرائب مستقلة؛ التعقيد الإضافي عند ضرائب مرتبطة بالدخل (القيد `ΔBS=-ΔG0+ΔT0+tΔY=0`).
- محاكي: سلايدرات T0، G0، R0، t (اختياري)؛ رسم BS مقابل Y وتحديد Y الذي تتوازن عنده الميزانية، مع تصنيف تلقائي (فائض/عجز)؛ قسم ثانٍ تفاعلي: إدخال ΔG0 مع قيد الحفاظ على BS، وحساب ΔT0 اللازم و ΔY الناتج، مقارنة بحالة "الميزانية المتوازنة" الكلاسيكية (ΔY=ΔG0 بالضبط عند ضرائب مستقلة).

**3.8 الميزان التجاري**
- نظرية: `NX=X-M=X0-M0-mY`؛ حالات التوازن/الفائض/العجز حسب مقارنة Y بـ `(X0-M0)/m`.
- محاكي: سلايدرات X0، M0، m؛ رسم NX مقابل Y مع تحديد نقطة التوازن ومنطقتي الفائض والعجز، مرتبط بنفس Y* المحسوب في محاكي 3.4 لإظهار وضع الميزان التجاري عند التوازن الكلي الفعلي للاقتصاد.

### الوحدة 4 — نموذج IS-LM

**4.1 اشتقاق منحنى IS**
- نظرية: دالة استثمار تابعة لسعر الفائدة `I=I0-µi`؛ التوازن `AD=AS` يعطي `IS: Y*=Ke[A-µi]` حيث `A` مجموع الإنفاق المستقل.
- محاكي: سلايدرات a، b، I0، µ، G0، T0، R0، X0، M0، t، m؛ رسم مزدوج (كما في المرجع: مخطط S,I أعلى يسارًا يعطي Y مقابل I، ثم مخطط IS أسفله) يوضح كيفية اشتقاق منحنى IS نقطة بنقطة من تغيّر سعر الفائدة i، مع معادلة IS المحدثة معروضة بصيغة `Y=Ke[A-µi]`.

**4.2 اشتقاق منحنى LM**
- نظرية: الطلب على النقود لثلاثة دوافع (معاملات `MD1=Q1+α1Y`، احتياط `MD2=Q2+α2Y`، مضاربة `MDt=A-gi`)؛ دالة الطلب الكلية `MD=Q+αY-gi`؛ عرض النقود `Ms0`؛ التوازن `LM: Y=1/α[Ms-Q+gi]`.
- محاكي: سلايدرات Q، α، g، Ms؛ رسم مزدوج مماثل (مخطط الطلب على النقود مقابل i، ثم مخطط LM) يوضح اشتقاق LM نقطة بنقطة.

**4.3 التوازن الآني وحالات منحنى LM**
- نظرية: تقاطع IS وLM يعطي (Y*, i*) بالصيغتين `Y*=ΘA+σM̄s` و `i*=(α/g)ΘA-(1/(Keg))Θ M̄s` حيث Θ المضاعف المالي وσ المضاعف النقدي؛ ثلاث مناطق لمنحنى LM: المجال الكينزي (أفقي، فخ السيولة، عند أدنى مستويات الفائدة)، المجال الكلاسيكي (عمودي، عند أعلى مستويات الفائدة، الطلب على النقود للمضاربة منعدم المرونة)، والمجال الوسيط.
- محاكي: مفتاح تبديل بين المناطق الثلاث (أو سلايدر مستمر لـ g يجعل LM ينتقل تدريجيًا من الأفقي إلى العمودي)؛ رسم IS وLM مع نقطة التوازن E، وشرح نصي تلقائي لطبيعة المرونة في المنطقة المختارة.

**4.4 فاعلية السياسة المالية والنقدية**
- نظرية: أثر السياسة المالية التوسعية (ΔG0>0) على إزاحة IS يمينًا؛ أثر السياسة النقدية التوسعية (ΔMs>0) على إزاحة LM يمينًا؛ الصيغ `ΔY=Θ·ΔA`, `Δi=（α/g)Θ·ΔA` للمالية، و `ΔY=σ·ΔMs`, `Δi=-(1/(Keg))Θ·ΔMs` للنقدية؛ فاعلية كل سياسة تتفاوت حسب منطقة LM (في المجال الكينزي: السياسة المالية فعّالة جدًا والنقدية عديمة الأثر على الدخل؛ في المجال الكلاسيكي: العكس تمامًا — "أثر المزاحمة" الكامل).
- محاكي: سلايدر لمقدار الصدمة (ΔG0 أو ΔMs، باختيار المستخدم) مع اختيار منطقة LM من 4.3؛ رسم "قبل/بعد" لإزاحة IS أو LM، وجدول يقارن ΔY وΔi الناتجين في المناطق الثلاث جنبًا إلى جنب ليرى الطالب التباين بعينه (يُعيد إنتاج جدول أثر أدوات السياسات في القسم الرابع من المرجع: مالية توسعية/انكماشية، نقدية توسعية/انكماشية، تجارية تقييدية/حرة).

**4.5 تنسيق السياسات الاقتصادية**
- نظرية: تطبيق سياسة مالية ونقدية معًا لتحقيق هدفين متزامنين (التشغيل الكامل مع مستوى فائدة مستهدف)؛ ثلاث حالات (E2: السياسة المالية أقوى، E4: النقدية أقوى، E3: تعادل الأثرين وثبات i).
- محاكي: سلايدرين منفصلين لـ ΔG0 وΔMs يُطبَّقان معًا؛ رسم إزاحة IS وLM في آن واحد ونقطة التوازن الجديدة E؛ التطبيق يصنّف تلقائيًا الحالة (E2/E3/E4) بمقارنة `(α/g)ΘΔA` مع `(1/(Keg))Θ·ΔMs` كما في صيغة القسم الخامس بالمرجع.

### الوحدة 5 — نموذج IS-LM-BP (موندل-فليمنغ)

**5.1 ميزان المدفوعات**
- نظرية: الصادرات `X=X0`، الواردات `M=M0+mY`، الميزان التجاري `NX=X-M`؛ الحساب الجاري وحساب رأس المال؛ ميزان المدفوعات = الحساب الجاري + حساب رأس المال.
- محاكي: سلايدرات X0، M0، m، ودرجة حركة رأس المال (حساسية تدفق رؤوس الأموال لفارق سعر الفائدة المحلي عن العالمي)؛ حساب NX وحساب رأس المال وميزان المدفوعات الكلي تلقائيًا مقابل Y وi.

**5.2 التوازن الداخلي والخارجي (منحنى BP)**
- نظرية: منحنى BP يمثل توفيقات (Y, i) التي تحقق توازن ميزان المدفوعات؛ ميله يعتمد على درجة حركة رأس المال (شبه أفقي عند حركة رأسمال مرتفعة جدًا، شبه عمودي عند حركة منعدمة)؛ التوازن الثلاثي عند تقاطع IS وLM وBP معًا.
- محاكي: يبني على محاكي 4.3 بإضافة منحنى BP قابل للتدوير (سلايدر درجة حركة رأس المال يغيّر ميل BP من شبه أفقي إلى شبه عمودي)؛ رسم IS وLM وBP معًا وتحديد نقطة التوازن الثلاثي، مع تصنيف المنطقة إذا كان الاقتصاد خارج توازن BP (فائض أو عجز في ميزان المدفوعات) بنفس منطق المناطق الأربع في نموذج IS-LM لكن بثلاثة منحنيات.

**5.3 السياسات الاقتصادية تحت أنظمة الصرف المختلفة**
- نظرية: أربع حالات (سياسة مالية/نقدية × سعر صرف ثابت/مرن)؛ تحت سعر الصرف الثابت السياسة النقدية عديمة الفعالية (لأن البنك المركزي يتدخل للحفاظ على السعر) بينما المالية فعّالة؛ تحت سعر الصرف المرن العكس صحيح.
- محاكي: قائمة اختيار (مالية توسعية / نقدية توسعية) × (صرف ثابت / صرف مرن) — أربع تركيبات؛ لكل تركيبة رسم متحرك لإزاحة IS أو LM ثم تعديل تلقائي إضافي (تدخل البنك المركزي أو تعديل سعر الصرف) يعيد الاقتصاد لمنحنى BP، مع شرح نصي تلقائي للنتيجة النهائية على Y مقارنة بالحالة المغلقة (IS-LM بدون BP).

**5.4 شرط مارشال-ليرنر**
- نظرية: `a+b>1` (a: مرونة الطلب الخارجي على الصادرات، b: مرونة الطلب المحلي على الواردات) شرط لنجاح سياسة تخفيض سعر الصرف في تحسين الميزان التجاري.
- محاكي: سلايدرين a وb؛ حساب المجموع فورًا وتصنيف: "الشرط محقق — تخفيض العملة يحسّن الميزان التجاري" أو "الشرط غير محقق — التخفيض قد يزيد العجز"، مع رسم تخطيطي لأثر التخفيض على قيمة الصادرات مقابل فاتورة الواردات في الحالتين.

### الوحدة 6 — الدورات الاقتصادية وآثار التغذية العكسية

**6.1 التغذية العكسية وآثار الصفقات التجارية بين دولتين**
- نظرية: زيادة واردات الدولة A من الدولة B ترفع دخل B، فترتفع واردات B من A (تسرب عائد جزئيًا)؛ أثر التغذية العكسية يُضعف المضاعف الفعلي للدولة A إذا لم يُؤخذ بالحسبان.
- محاكي: نموذج دولتين مبسّط (A وB) بمعاملات ميل حدي للاستيراد `mA`, `mB`؛ صدمة إنفاق مستقل في A، والتطبيق يحاكي عدة جولات من التأثير المتبادل (A→B→A→...) ويرسم تقارب الدخل في كلا الدولتين نحو التوازن الجديد، مقارنة بمضاعف الاقتصاد المغلق البسيط لإظهار حجم "التسرب" بسبب التغذية العكسية.

### الوحدة 7 — نماذج النمو الاقتصادي

**7.1 نموذج سولو**
- نظرية: `Y=F(K,L)`، تراكم رأس المال `ΔK=sY-δK`، حالة الاستقرار (steady state) عند `sf(k)=δk` (بدلالة رأس المال للعامل الواحد k=K/L).
- محاكي: سلايدرات معدل الادخار s، معدل الإهلاك δ، معدل نمو السكان n، ودالة إنتاج كوب-دوغلاس `y=k^α`؛ رسم منحنيي sy وδk (أو (δ+n)k) وتحديد حالة الاستقرار k* تلقائيًا، مع محاكاة المسار الزمني لـ k بدءًا من قيمة ابتدائية يختارها المستخدم حتى التقارب.

**7.2 نماذج النمو الداخلي**
- نظرية: نموذج رومر `Y=AK^αL^(1-α)` (المعرفة/التكنولوجيا عامل داخلي)؛ نموذج لوكاس `Y=AK^α(L·H)^β` (رأس المال البشري)؛ نموذج أكيلانو وهاويت `Y=AK^αL^(1-α)` بمنطق التدمير الخلّاق (الابتكار يستبدل التقنيات القديمة عبر المنافسة).
- محاكي: مفتاح تبديل بين النماذج الثلاثة؛ سلايدرات A، K، L، H (عند لوكاس)، α، β؛ حساب Y فوريًا ورسم أثر زيادة كل عامل (A أو H أو K) على Y بمعزل عن الآخرين لإبراز الفارق الجوهري بين النماذج (رومر: التكنولوجيا A هي المحرك؛ لوكاس: الحاصل L·H هو المحرك؛ أكيلانو-هاويت: نفس شكل رومر لكن مع سرد نصي توضيحي لآلية "التدمير الخلّاق" المولّدة لتغيّر A عبر الزمن).

## 7. تجربة المستخدم العامة (لكل صفحة)

نفس البنية المعتمدة في تطبيق الاقتصاد الجزئي (فكرة في سطرين → إطار رياضي → محاكي → اختبر فهمك)، مع إضافة **خاصة بهذا التطبيق**: كل صفحة سياسات (4.4، 4.5، 5.3) تعرض دائمًا لوحة "قبل / بعد / الفرق Δ" ثابتة الموضع أعلى الرسم، لأن مقارنة الحالتين هي جوهر التعلّم في هذه الفصول.

## 8. متطلبات غير وظيفية

نفس متطلبات تطبيق الاقتصاد الجزئي (القسم 8 في PRD_تطبيق_الاقتصاد_الجزئي_المعمق.md): دعم RTL كامل، أداء تحت 200ms لكل محاكي، بدون قاعدة بيانات، قابل للنشر عبر `streamlit run app.py` أو Streamlit Community Cloud.

## 9. معايير القبول (Acceptance Criteria)

- [ ] كل وحدة من الوحدات السبع تحتوي كل الدروس المذكورة في القسم 6، كل درس في صفحة مستقلة.
- [ ] محاكي 3.2 يُعيد نتائج متطابقة مع أي مثال عددي تحقق من الصيغة `Y*=1/(1-b)[a+I0]` يدويًا.
- [ ] محاكي 4.3 (التوازن الآني) يُعيد نتائج متطابقة مع الصيغتين المغلقتين `Y*=ΘA+σM̄s` و`i*=(α/g)ΘA-(1/Keg)ΘM̄s` للمدخلات نفسها.
- [ ] محاكي 5.2/5.3 قادر على إعادة حل تمرين IS-LM-BP كامل (استخراج معادلتي IS وLM، حساب Y* وi* التوازنيين، حساب الميزان التجاري NX، حساب سعر الصرف التوازني أو رصيد الميزانية BS، حساب المضاعف والمضاعف النقدي) بالشكل الذي يُطلب في تمارين المقياس الجامعي القياسية.
- [ ] محاكي 3.7 يُعيد نتيجة "أثر الميزانية المتوازنة" ΔY=ΔG0=ΔT0 بدقة عند ضرائب مستقلة.
- [ ] محاكي 4.4 يُصنّف بشكل صحيح فعالية كل سياسة (مالية/نقدية) في كل من المجالات الثلاثة لمنحنى LM (كينزي/وسيط/كلاسيكي) طبقًا للجدول المرجعي.
- [ ] كل الواجهة بالعربية RTL بدون أي نص إنجليزي إلا الرموز الرياضية القياسية.

## 10. خارطة الطريق (Milestones)

| المرحلة | المحتوى |
|---|---|
| M0 | الهيكل العام + دعم RTL + نظام تنقل (يُعاد استخدام utils/rtl.py من تطبيق الاقتصاد الجزئي إن كان نفس المشروع) |
| M1 | الوحدتان 1 و2 (مدخل + حسابات وطنية + كلاسيكي/كينزي) |
| M2 | الوحدة 3 كاملة (8 دروس — النموذج الكينزي البسيط 2/3/4 قطاعات والمضاعفات والميزانية) |
| M3 | الوحدة 4 كاملة (5 دروس — IS-LM) |
| M4 | الوحدة 5 كاملة (4 دروس — IS-LM-BP) |
| M5 | الوحدتان 6 و7 (الدورات الاقتصادية + نماذج النمو) |
| M6 | تلميع عام: اختبارات فهم، تتبع تقدّم، تحسين أداء، نشر |

## 11. مخاطر تقنية

- **تعقيد الاشتقاق الجبري لأنظمة IS-LM-BP الثلاثية**: عدد المعاملات كبير (a,b,I0,µ,G0,T0,R0,X0,M0,t,m,Q,α,g,Ms، بالإضافة لدرجة حركة رأس المال) ⇒ الحل: بناء `utils/solvers.py` بدالة عامة تحل أي نظام خطي من معادلتين أو ثلاث معادلات بمتغيرين/ثلاثة عبر numpy.linalg.solve، بدل اشتقاق sympy رمزي كامل في كل صفحة؛ يُحتفظ بـsympy فقط لعرض خطوات الحل النصية على دوال قياسية محفوظة مسبقًا (منسوخة حرفيًا من المرجع) بدل اشتقاقها ديناميكيًا في كل مرة.
- **دقة تصنيف "منطقة LM" (كينزي/وسيط/كلاسيكي)**: هذا التصنيف في المرجع وصفي (نوعي) وليس بعتبة رقمية صريحة لـ g ⇒ الحل: استخدام عتبات نسبية للمستخدم يختارها صراحة (مفتاح تبديل) بدل استنتاجها آليًا من قيمة g عشوائية، لتفادي تصنيف خاطئ لا يطابق ما توقعه الطالب.
- **حجم الرسوم المزدوجة (اشتقاق IS وLM من رسمين متراكبين)**: يتطلب تنسيق plotly لعرض رسمين فرعيين (subplots) متزامنين مع تفاعل مشترك ⇒ اختبار الأداء مبكرًا (Milestone M3) قبل تعميم النمط على بقية الدروس.
